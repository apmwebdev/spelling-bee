import { LetterHintSubsectionProps } from "../LetterHintPanel"

export function WordCountList({
  answers,
  correctGuessWords,
  numberOfLetters,
  locationInWord,
  offset,
}: LetterHintSubsectionProps) {
  return <div>WordCountList</div>
}
