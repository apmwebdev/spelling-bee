import { LetterHintSubsectionProps } from "../LetterHintPanel"

export function LettersOnly({
  answers,
  correctGuessWords,
  numberOfLetters,
  locationInWord,
  offset,
}: LetterHintSubsectionProps) {
  return <div>LettersOnly</div>
}
